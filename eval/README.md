# Evaluation datasets and configurations for agents-cli eval
#
# Usage:
#   agents-cli eval dataset synthesize   # Generate eval scenarios
#   agents-cli eval generate             # Run agent on dataset
#   agents-cli eval grade                # Grade responses
#   agents-cli eval compare              # Regression diff
#   agents-cli eval analyze              # Cluster failures
#   agents-cli eval optimize             # Auto-tune prompts
#
# Add .jsonl eval datasets here once Phase 3 begins.
